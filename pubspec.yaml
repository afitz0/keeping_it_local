name: keeping_it_local
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  http: 0.12.2
  flutter:
    sdk: flutter
  provider: 4.3.2+2
  shared_preferences: 0.5.12
  cached_network_image: 2.3.2+1
  sqflite: 1.3.1+1
  editable: 1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true